# 🏗️ My NFTs Feature Architecture

## 📐 System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌───────────────┐    ┌──────────────┐    ┌─────────────────┐  │
│  │  /mynfts      │    │  /nftlist    │    │   Navbar        │  │
│  │  page.tsx     │    │  page.tsx    │    │   (My NFTs)     │  │
│  └───────┬───────┘    └──────┬───────┘    └─────────────────┘  │
│          │                   │                                   │
│          └───────────────────┴──────────────────┐               │
│                                                  │               │
│                         ┌────────────────────────▼─────────────┐│
│                         │   MyNFTsList.tsx Component           ││
│                         │  - Display NFTs with images          ││
│                         │  - List/Unlist functionality         ││
│                         │  - Detail modal                      ││
│                         └────────────┬─────────────────────────┘│
│                                      │                           │
└──────────────────────────────────────┼───────────────────────────┘
                                       │
┌──────────────────────────────────────▼───────────────────────────┐
│                      HOOKS & LOGIC LAYER                          │
├───────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │              lib/nft-hooks.ts                                │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │ │
│  │  │ useMyNFTs()  │  │ useAllNFTs() │  │ useNFTsForSale() │  │ │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬───────────┘  │ │
│  │         │                 │                  │              │ │
│  │         └─────────────────┴──────────────────┘              │ │
│  │                           │                                 │ │
│  │              ┌────────────▼─────────────┐                   │ │
│  │              │  useReadContract (wagmi) │                   │ │
│  │              │  - Calls smart contract  │                   │ │
│  │              └────────────┬─────────────┘                   │ │
│  └─────────────────────────────┼───────────────────────────────┘ │
│                                │                                 │
│  ┌─────────────────────────────▼───────────────────────────────┐ │
│  │              lib/nft-utils.ts                                │ │
│  │  - formatNFTPrice()     - sortNFTs()                         │ │
│  │  - shortenAddress()     - filterNFTsByStatus()               │ │
│  │  - getNFTStats()        - searchNFTs()                       │ │
│  │  - calculateTotalValue() - groupNFTsByCreator()              │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────┐
│                    BLOCKCHAIN LAYER                               │
├───────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │              Smart Contract Functions                        │ │
│  │                                                              │ │
│  │  viewMyNFTs() ──────► Returns NFT[] owned by msg.sender     │ │
│  │  viewAllNFTs() ─────► Returns all NFTs in marketplace       │ │
│  │  viewNFTsForSale() ─► Returns NFTs listed for sale          │ │
│  │  listNFTForSale() ──► List an NFT for sale                  │ │
│  │  unlistNFT() ───────► Remove NFT from sale                  │ │
│  │  buyNFT() ──────────► Purchase an NFT                       │ │
│  │                                                              │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────┐
│                      IPFS / PINATA LAYER                          │
├───────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │              lib/pinata.ts                                   │ │
│  │                                                              │ │
│  │  fetchImageUrl(cid) ──────► Returns signed Pinata URL       │ │
│  │  uploadFileToIPFS(file) ──► Uploads file, returns CID       │ │
│  │                                                              │ │
│  │  IPFS Storage:                                               │ │
│  │  - NFT Images                                                │ │
│  │  - Metadata JSON                                             │ │
│  │                                                              │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

### Fetching NFTs

```
User Opens /mynfts
       │
       ▼
MyNFTsList Component
       │
       ▼
useMyNFTs() Hook
       │
       ▼
useReadContract (Wagmi)
       │
       ▼
Smart Contract: viewMyNFTs()
       │
       ▼
Returns: NFT[] Array
       │
       ├──► Extract Image CIDs
       │         │
       │         ▼
       │    fetchImageUrl(cid)
       │         │
       │         ▼
       │    Pinata API
       │         │
       │         ▼
       │    Signed URLs
       │         │
       └─────────┴──► Combine Data
                      │
                      ▼
              Display NFTs with Images
```

### Listing NFT for Sale

```
User Clicks "List for Sale"
       │
       ▼
Open List Modal
       │
       ▼
User Enters Price
       │
       ▼
useWriteContract (Wagmi)
       │
       ▼
Smart Contract: listNFTForSale(nftId, price)
       │
       ▼
Wait for Transaction Confirmation
       │
       ▼
Refetch NFTs (useMyNFTs().refetch())
       │
       ▼
UI Updates Automatically
```

---

## 🗂️ File Structure

```
frontend/
│
├── app/
│   ├── mynfts/
│   │   └── page.tsx                   🎯 My NFTs dedicated page
│   │
│   └── nftlist/
│       └── page.tsx                   📋 Marketplace (has "Owned" filter)
│
├── components/
│   ├── MyNFTsList.tsx                 🎨 Reusable NFT list component
│   ├── Navbar.tsx                     🧭 Navigation (updated)
│   └── ui/
│       └── nft-card-detail.tsx        💳 NFT card detail component
│
├── lib/
│   ├── nft-hooks.ts                   🪝 React hooks for NFT data
│   ├── nft-utils.ts                   🛠️  Utility functions
│   ├── contract.ts                    📝 Contract address & ABI
│   ├── pinata.ts                      🌐 IPFS/Pinata integration
│   ├── abi.json                       📜 Smart contract ABI
│   └── wagmi.ts                       ⚙️  Wagmi configuration
│
├── examples/
│   └── nft-utils-examples.tsx         📚 Usage examples
│
└── docs/
    ├── MY_NFTS_DOCUMENTATION.md       📖 Full documentation
    ├── MYNFTS_SUMMARY.md              📋 Quick summary
    ├── QUICK_REFERENCE.md             ⚡ Quick reference
    └── ARCHITECTURE.md                🏗️  This file
```

---

## 🔌 Component Relationships

```
┌──────────────────────────────────────────────────────────────────┐
│                         Pages                                     │
│                                                                   │
│  ┌────────────────┐          ┌────────────────┐                  │
│  │  /mynfts       │          │  /nftlist      │                  │
│  │                │          │                │                  │
│  │  Uses:         │          │  Uses:         │                  │
│  │  MyNFTsList ◄──┼──────────┤  useMyNFTs     │                  │
│  │                │          │  useAllNFTs    │                  │
│  │                │          │  useNFTsForSale│                  │
│  └────────────────┘          └────────────────┘                  │
└──────────────────────────────────────────────────────────────────┘
                                │
                                │ imports
                                │
┌───────────────────────────────▼──────────────────────────────────┐
│                     Reusable Components                           │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  MyNFTsList.tsx                                              │ │
│  │                                                              │ │
│  │  Imports & Uses:                                             │ │
│  │  • useMyNFTs() ──────────► Fetch NFT data                   │ │
│  │  • useWriteContract() ───► List/Unlist NFTs                 │ │
│  │  • formatEther() ─────────► Format prices                   │ │
│  │  • NFT type ──────────────► TypeScript types                │ │
│  │                                                              │ │
│  └─────────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────────┘
                                │
                                │ uses
                                │
┌───────────────────────────────▼──────────────────────────────────┐
│                        Hooks Layer                                │
│                                                                   │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────────────┐ │
│  │useMyNFTs()   │   │useAllNFTs()  │   │useNFTsForSale()      │ │
│  │              │   │              │   │                      │ │
│  │Fetches:      │   │Fetches:      │   │Fetches:              │ │
│  │viewMyNFTs()  │   │viewAllNFTs() │   │viewNFTsForSale()     │ │
│  │              │   │              │   │                      │ │
│  │Returns:      │   │Returns:      │   │Returns:              │ │
│  │• myNFTs[]    │   │• allNFTs[]   │   │• forSaleNFTs[]       │ │
│  │• isLoading   │   │• isLoading   │   │• isLoading           │ │
│  │• getImageUrl │   │• getImageUrl │   │• getImageUrl         │ │
│  │• refetch     │   │• refetch     │   │• refetch             │ │
│  └──────────────┘   └──────────────┘   └──────────────────────┘ │
└───────────────────────────────────────────────────────────────────┘
                                │
                                │ calls
                                │
┌───────────────────────────────▼──────────────────────────────────┐
│                      Utility Layer                                │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  nft-utils.ts                                                │ │
│  │                                                              │ │
│  │  Formatting:              Filtering:           Stats:        │ │
│  │  • formatNFTPrice()       • filterByStatus()   • getNFTStats│ │
│  │  • shortenAddress()       • filterByPrice()    • calculate  │ │
│  │  • formatDate()           • searchNFTs()       • totals     │ │
│  │                                                              │ │
│  │  Sorting:                 Other:                             │ │
│  │  • sortNFTs()             • isOwner()                        │ │
│  │  • groupByCreator()       • isCreator()                      │ │
│  └─────────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────────┘
```

---

## 🎭 State Management

```
┌────────────────────────────────────────────────────────────────┐
│                    Component State                              │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│  MyNFTsList.tsx:                                                │
│  ├─ selectedNFT: NFT | null         (Currently viewing)        │
│  ├─ listPrice: string                (Price input)             │
│  ├─ showListModal: boolean           (Modal visibility)        │
│  └─ imageUrls: Record<string,string> (Cached Pinata URLs)      │
│                                                                 │
├────────────────────────────────────────────────────────────────┤
│                    Hook State                                   │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│  useMyNFTs():                                                   │
│  ├─ myNFTs: NFT[]                    (From blockchain)         │
│  ├─ isLoading: boolean               (Loading state)           │
│  ├─ isConnected: boolean             (Wallet status)           │
│  └─ imageUrls: Record<string,string> (IPFS image URLs)         │
│                                                                 │
├────────────────────────────────────────────────────────────────┤
│                  Transaction State                              │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│  useWriteContract():                                            │
│  ├─ hash: Hash                       (Transaction hash)        │
│  ├─ isPending: boolean               (Tx pending)              │
│  └─ isConfirming: boolean            (Waiting confirmation)    │
│                                                                 │
└────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Lifecycle Flow

### Component Mount

```
1. Component Mounts
   └─► useMyNFTs() initializes
       └─► useReadContract() calls viewMyNFTs()
           └─► Returns NFT[] from blockchain
               └─► useEffect() triggered
                   └─► Fetch images from Pinata
                       └─► Update imageUrls state
                           └─► Component re-renders with images
```

### User Interaction (List NFT)

```
1. User clicks "List for Sale"
   └─► setShowListModal(true)
       └─► User enters price
           └─► User clicks "List"
               └─► writeContract() called
                   └─► Transaction sent to blockchain
                       └─► isPending = true (UI shows loading)
                           └─► Transaction confirmed
                               └─► isConfirmed = true
                                   └─► refetch() called
                                       └─► Fresh data fetched
                                           └─► UI updates
```

---

## 🎨 UI Component Tree

```
MyNFTsList
│
├── Wallet Check
│   └── "Connect Wallet" message (if not connected)
│
├── Loading State
│   └── Spinner + "Loading..." message
│
├── Empty State
│   └── "No NFTs Found" message
│
└── NFT Grid
    │
    ├── NFT Card (for each NFT)
    │   ├── Image
    │   ├── NFT Info
    │   │   ├── Name
    │   │   ├── Owner Address
    │   │   ├── Price
    │   │   └── "Owned" Badge
    │   │
    │   └── Action Button
    │       ├── "List for Sale" (if not listed)
    │       └── "Unlist" (if listed)
    │
    ├── Detail Modal (when NFT selected)
    │   ├── Large Image
    │   ├── NFT Details
    │   │   ├── Name & ID
    │   │   ├── Description
    │   │   ├── Creator
    │   │   ├── Owner
    │   │   ├── Royalties
    │   │   ├── Times Sold
    │   │   └── Price
    │   │
    │   └── Close Button
    │
    └── List Price Modal (when listing)
        ├── NFT Name
        ├── Price Input
        └── "List for Sale" Button
```

---

## 🚀 Performance Considerations

### Optimizations Implemented

1. **Image Caching**
   - Signed URLs cached in state
   - Prevents redundant Pinata API calls

2. **Conditional Fetching**
   - Only fetch when wallet connected
   - Skip if no NFTs exist

3. **Parallel Image Loading**
   - All images fetched concurrently
   - Non-blocking UI

4. **Smart Contract Calls**
   - Read-only calls (no gas)
   - Automatic caching by Wagmi

---

## 📊 Data Models

### NFT Interface
```typescript
interface NFT {
  id: bigint;              // Token ID
  name: string;            // NFT name
  description: string;     // NFT description
  imageHash: string;       // IPFS CID or URL
  price: bigint;           // Price in wei
  royalties: bigint;       // Royalty percentage
  creator: Address;        // Creator address
  currentOwner: Address;   // Current owner address
  isForSale: boolean;      // Sale status
  timesSold: bigint;       // Sales count
}
```

---

## 🎯 Key Features Summary

✅ **Smart Contract Integration** - Calls `viewMyNFTs()`  
✅ **Image Display** - IPFS via Pinata with signed URLs  
✅ **List/Unlist** - Full marketplace functionality  
✅ **Reusable Components** - Use anywhere in app  
✅ **Type Safety** - Full TypeScript support  
✅ **Error Handling** - Graceful error states  
✅ **Loading States** - Beautiful loading UI  
✅ **Responsive Design** - Mobile-friendly  
✅ **Animation Effects** - Smooth hover & transitions  

---

**Ready to use! Check `/mynfts` to see it in action.** 🎉
